# 项目协作规范

## 硬性文档要求

- 本项目中的所有工作过程，包括但不限于需求澄清、资料调研、技术决策、架构设计、实施计划、开发记录、测试验证、问题排查、风险分析与后续变更，都必须及时、如实地记录到项目根目录的 `docs/` 目录中。
- 除本文件 `AGENTS.md` 外，不得把项目过程文档分散到其他目录；新增文档应放入 `docs/`，并在需要时更新 `docs/README.md` 索引。
- 不得仅在聊天回复中记录关键结论；影响实现的结论、未决问题、来源与验证结果必须同步落盘到 `docs/`。
